#pragma once

// common types and definitions for design and model layer

////////////////////////////////////////////////////////////
// Source
////////////////////////////////////////////////////////////

enum class UndulatorSigmaType { Standard, Accurate };

enum class ElectronEnergyOrientation { Clockwise, Counterclockwise };

////////////////////////////////////////////////////////////
// Element
////////////////////////////////////////////////////////////

/// Behavior of element when a ray hits the back side of the element (i.e., the side opposite to the normal direction).
enum class BackFaceBehavior {
    Ignore,
    Absorb,
};

////////////////////////////////////////////////////////////
// Curvature
////////////////////////////////////////////////////////////

enum class ToroidType {
    Convex,
    Concave,
};
