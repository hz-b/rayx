#version 450
#extension GL_ARB_separate_shader_objects : enable

#define WORKGROUP_SIZE 32
layout (local_size_x = WORKGROUP_SIZE, local_size_y = 1, local_size_z = 1 ) in;

layout(std140, binding = 0) buffer obuf
{
   uint outputData[];
};
layout(std140, binding = 1) buffer ibuf
{
   uint rayData[];
};


void main(){
	//outputData[uint(gl_GlobalInvocationID)] = gl_GlobalInvocationID[0];
}